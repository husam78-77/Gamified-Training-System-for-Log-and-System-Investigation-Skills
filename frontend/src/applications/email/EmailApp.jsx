import React from 'react';

/**
 * EmailApp.jsx
 * Placeholder only — no business logic, no API calls, no state.
 */
const EmailApp = () => {
    return <div>Email Application</div>;
};

export default EmailApp;
