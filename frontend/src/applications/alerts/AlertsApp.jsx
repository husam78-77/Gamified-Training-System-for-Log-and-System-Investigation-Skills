import React from 'react';

/**
 * AlertsApp.jsx
 * Placeholder only — no business logic, no API calls, no state.
 */
const AlertsApp = () => {
    return <div>Alerts Application</div>;
};

export default AlertsApp;
