import React from 'react';

/**
 * WindowBody.jsx
 * Simple content container — every application's content renders here.
 */
const WindowBody = ({ children }) => {
    return <div className="window-body">{children}</div>;
};

export default WindowBody;
